gastos_publicos:
  target: dev

  outputs:
    dev:
      type: duckdb
      path: ":memory:"
      schema: main
      threads: 4

      extensions:
        - ducklake

      attach:
        - path: "ducklake:{{ env_var('PWD') }}/data/lakehouse/catalog/gastos_publicos.ducklake"
          alias: gastos_publicos
          options:
            data_path: "{{ env_var('PWD') }}/data/lakehouse/files/"
